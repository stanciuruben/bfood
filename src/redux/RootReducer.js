const initialState = {
    userName: null,
    cart: [],
    orders: []
}

const RootReducer = ( state = initialState, action ) => {
    switch ( action.payload ) {
        default: return state;
    }
}